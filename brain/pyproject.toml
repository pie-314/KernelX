[project]
name = "kernelx-openenv"
version = "0.1.0"
description = "KernelX OpenEnv Client and Server"
dependencies = [
    "openenv-core",
    "pydantic",
    "numpy",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
